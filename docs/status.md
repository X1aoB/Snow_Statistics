# 实施状态

本记录只列实际证据，不把技术栈配置文件计作集群验收。2026-09-11 首轮实现。

本次交付是可运行首版；完整计划仍有后续实施和集群验收。轻量服务、产品候选适配、可靠同步及离线正确性样例已落地。

## 已验证

- 独立公开仓库 `X1aoB/Snow_Statistics` 已创建。
- Python 81 项测试通过：包含血缘恢复、同步目的地绑定/持续轮询与新鲜度完整覆盖检查。浏览器接口契约仍保持 v1。
- 浏览器 7 项测试通过：默认关闭无标识/网络副作用、隐私信号、缺失/不安全的采集地址、重复入口点击更新归因标识、有限队列和重试、统计故障不伪造零值、归档/文本渲染。
- MyWebsite 独立工作树构建与 Astro 检查通过，60 项测试通过；Project_Snow 前端类型检查和 7 项 Node 测试通过，两个产品采集仍默认关闭。
- Project_Snow 两项实际 Chromium 测试通过：默认关闭无统计标识/请求；生产 CSP 下采集 503、断连、超时均不阻断假后端聊天，撤销后统计标识及回调清除。7 项静态发布包验证通过；没有付费模型调用。
- 轻量 Docker 容器在独立 2 GiB ext4 文件系统中通过实际验收：无数仓运行仍刷新、强制 kill 后恢复去重、full→lite 指标不变、真实 ENOSPC 后拒收且已确认事件保留、off 显示归档且不删除历史。测试容器已移除，测试数据库镜像保留在忽略的 runtime。
- Flink Java 作业用 Maven 3.9.9 编译成功，8 项归一化/版本溢出/来源隔离/严格完成事件/时间精度/迟到边界测试通过。Java 11 字节码、Flink 1.20.3、Doris connector 25.1.0、Jackson BOM 2.17.3；20 个 runtime JAR 的 SHA256 加入 CI 校验。
- 三台 VMware VMX/薄置备磁盘/seed 已生成；Ubuntu release-20260826 SHA256 已验证。6/6/10 GiB 节点已逐台通过实际启动、cloud-init、受验证 SSH 和 Docker 初始化；未把逐台启动当作三节点并发负载验收。
- 控制节点 Kafka 3.9.1、MySQL 8.4.5、Debezium 3.0.8.Final 启动。真实 Kafka 收发 28 条合成事件；连接器及任务 RUNNING，两个模拟工单写入成功。
- CDC 归档覆盖 contents/campaigns/tickets，首轮取得 19 条唯一变更，包含 1 条删除和 15 条有事务元数据的变更。
- 真 Kafka 中断恢复：接收 28 条，5 条确认后模拟失败；恢复重放后共 33 次 Kafka 确认，最终源位点 28，公开合成行数 0。
- Spark 3.5.7/Java 11/Python 3.8 容器实际运行：56 条含重放输入 → 28 条有效 + 28 条重复，隔离 0；事件分层和运营 SCD2/工单作业完成。模式为单虚拟机 local[2]，不是 YARN 集群成绩。
- 实际 Spark Parquet 与正确性基准对账通过：6 行日指标、4 个工单轮次、4 个内容有效版本。
- Iceberg 1.10.0 在实际 Spark 容器中通过 MERGE、历史快照读取、字段新增、分区从月到日演进、数据文件重写；独立本地文件系统 catalog，未冒充 HDFS 湖仓集群。
- Doris 3.0.6.2 在分析节点受限容器中通过建表、稳定主键重放、旧业务版本不覆盖新版本、实时/离线整数对账，并在重建容器后重复通过。FE 上限 2.5 GiB、BE 上限 5 GiB，首次小样本后观察约 889 MiB/1.105 GiB 使用量；这是单键功能验收，不代表负载测试峰值。
- Python 正确性基准：10 万条约 6.618 秒；100 万条约 89.844 秒；均完整对账且隔离 0。固定 seed 42，不能作为分布式吞吐或真实用户规模。
- 14 个第三方镜像已从实际 registry 解析并固定 digest；Ubuntu、Python lock 与 Java 直接版本可检查。

## 后续离线闭环验收（2026-09-11）

- HDFS 3.4.1 两个 DataNode、YARN RM/NM、Hive 3.1.3 元数据库实际联通；元数据库持久卷重建容器后校验通过。Hive 客户端 334 个 JAR 由固定镜像提取并逐一 SHA256 锁定。
- Spark 3.5.7 在真实 YARN 上运行，执行器位于 compute，输入/输出位于 HDFS，Hive 元数据注册及查询成功。56 条输入 → 28 有效 + 28 重复，0 隔离，6 行日指标与基准一致。
- 重跑指标相同；故意制造基准不一致时不生成 accepted 标记，旧结果仍存在，新的 run ID 重算恢复。输出 17 个非空块均有两个有效副本；停止一个 DataNode 后读回输入 SHA256 相同。
- Doris 正式发布改为不可变日快照及原子日期指针。实际通过读回对账、重复发布、提交前失败保留旧报表、空日期修正、较旧 cutoff 拒绝覆盖和相同 cutoff 冲突拒绝。
- Streamlit 已查询 Doris 已发布视图；AppTest 验证实际 6 行指标、基准切换及不可用提示，真实 Chromium 验证两张表和趋势图渲染。高级分析基准仍可独立查看。
- Airflow 2.10.5 实际 DAG `synthetic-e2e-20260911` 成功：window 成功，YARN 计算首次成功，Doris 停止时发布进入重试，资源切换后第二次发布成功；计算没有重跑。默认手动模式，仅常驻 scheduler，受限 SSH 网关复用固定 Spark 运行时。
- 初始独立 YARN 样例使用 VM 4/6/2 GiB、executor 1 GiB；Airflow 链路已使用 4/4/2 GiB、executor 768 MiB。发布阶段 control 4 GiB、compute 关闭、analysis 6 GiB，Doris FE/BE 容器上限 2/3 GiB。资源切换由操作者完成，Airflow 不管理 VMware。

收据见 `evidence/yarn.json`、`evidence/publication.json`、`evidence/airflow.json`；操作见 [离线闭环手册](offline-pipeline.md)。这是固定小样本离线闭环，尚不是全部技术栈或连续线上链路的验收。

原始验收工具输出的汇总收据见 [evidence](evidence/README.md)。业务适配为独立草稿 PR：[MyWebsite #2](https://github.com/X1aoB/MyWebsite/pull/2)、[Project_Snow #59](https://github.com/X1aoB/Project_Snow/pull/59)。未合并或部署。

## 增量 ODS 验收（2026-09-11）

- Kafka 历史范围捕获 81 条合成原始消息：61 行行为、19 行 CDC、1 条 tombstone、隔离 0。四个分区独立记录边界、cluster/Topic ID；Kafka group 在 HDFS 验证前保持未提交。
- WebHDFS 原子目录落地、SHA256 读回、两个副本确认、固定输入清单已实际通过。注入 HDFS 批次提交后失败，重试仍为相同清单；注入 Kafka ACK 后本地 checkpoint 前失败，恢复后位点相同、空增量不生成新批次。
- 第二次追加 3 条重放事件，只捕获 offset 61..63；清单累积两个批次，原清单保持不变。
- 第一份 Kafka 输入清单已接入真实 YARN/Hive：61 行 → 28 有效 + 33 重复，0 隔离，6 行日指标与基准一致。
- 两批累计输入再次在 YARN/Hive 计算：64 行 → 28 有效 + 36 重复，日指标与第一轮完全一致；Doris 发布及重复发布、公开实验视图读回一致。第二批位点已提交，之后捕获无新数据，pending 已清除。
- CDC 运营模型已接入 HDFS/YARN：19 条变更形成 4 个 SCD2 内容版本、4 个工单处理轮次、8 行每日状态（截至 2026-01-04），Parquet 读回与相同日期的基准一致。首次严格校验发现旧基准只生成到末次变更次日，现提供显式 `operations_as_of` 并测试未来变更排除；旧失败运行不生成 accepted 标记。
- 首轮资源 4/4/2 GiB。某次重算时临时 JAR 触发 60 GiB 容量门禁，作业已中断；增加 `--reserve-mib 1024` 作业启动检查，新增 `ods` 3.5/4/1 GiB 配置与分析 DataNode 512 MiB 容器。容量阈值保持原值，没有清理已接收数据。
- 3.5/4/1 GiB 配置实际完成增量复算；日指标 17 个非空块、运营模型 4 个非空块均至少双副本、fsck HEALTHY。结束时项目文件约 58.91 GiB，1 GiB 分析 VM 的 DataNode 观察到约 216.8 MiB 容器用量、434 MiB guest 可用内存，非压力峰值。Doris 发布阶段仅保留 6 GiB 分析 VM，由 Windows 验证客户端执行，控制机关闭。

操作与恢复边界见[增量 ODS 手册](incremental-ods.md)。实际运行收据在 `evidence/incremental-ods.json`。这是合成数据、单写者、显式位点及业务去重的验收，不是跨系统事务或持续实时 SLA。

## 运营与行为模型验收（2026-09-11）

- 新增 Spark 分布式会话、D1 / D7 留存、最近入口转化；会话读取报表前历史，跨午夜不拆分，恰好 30 分钟静默开始新会话。留存观察期未结束时返回 null，并按实际成熟分母展示。主转化允许默认角色直接对话，角色选择仅为辅助诊断；指标字典更新为 v2，事件和公开汇总接口保持 v1。
- 31 条边界样例在真实 YARN 上得到 27 有效 + 4 重复，0 隔离；13 个会话、5 行会话日汇总、3 个留存分群、2 次有效转化及 1 行渠道汇总。五张 Parquet 表逐行与独立循环基准一致，Hive 注册与读回通过。包含晚 1 毫秒失效、最近点击不回退、默认角色、跨天及未成熟留存。
- CDC 运营作业接入 Hive 与受限网关；Airflow `snow_models` 固定输入清单，依次执行运营、行为、私有成对发布。`models-e2e-20260911` 与 `models-replay-20260911` 的四个任务全部首次成功。两个运行结果哈希一致。
- 固定两批 ODS 清单仍是 64 行行为 → 28 有效 + 36 重复、19 条 CDC。运营形成 4 个 SCD2 版本、4 个处理轮次、8 行日状态；行为形成 11 个会话、6 行会话日汇总、2 个留存分群、1 次有效转化、3 行渠道汇总。发布汇总及模型计数通过独立基准对账；此检查不冒充所有运营明细在这次 DAG 内逐行黄金校验，运营明细黄金证据见前轮。
- 私有发布器通过字段白名单、粒度、指标不等式、成熟期、质量计数与同清单/同窗口检查；失败或同截止时间冲突保留旧发布。网关复用已验收的同一任务，拒绝改变窗口或访问非固定清单。新增独立资源登记，暂停不会删除模型或历史。
- Spark/YARN 改为两端预装 252 个已锁定 JAR，启动时逐一校验；边界作业只上传 pyspark.zip、py4j 和配置包，没有上传 `__spark_libs__`。相同 3.5 / 4 / 1 GiB 三 VM 连续完成六次 Spark 作业，包括两个 Airflow DAG；运行末项目文件约 58.93 GiB。控制 VM 在 Airflow + Spark 一次观察中仍有 1036 MiB 可用内存，非压力峰值。
- 原有日指标作业使用共享去重模块后再次通过真实 YARN/Hive 黄金回归：64 → 28 + 36，6 行日指标。新模型目录 20 个非空块、边界结果 6 个、日指标结果 17 个，均双副本且 fsck HEALTHY。
- Streamlit 新增实际 Spark 私有归档视图；AppTest 与真实 Chromium 验证五张表、未成熟 D7 提示和数据源切换。无需查询虚拟机，历史日期与数据截止时间明确显示。未接入产品公开页或生产采集。
- 收尾时两个 Airflow DAG 均暂停，所有实验容器及三台 VM 已正常停止，数据保留；项目文件约 50.43 GiB，宿主空闲约 59.78 GiB。

操作见[运营与行为模型](behavior-models.md)，原始工具汇总收据见 `evidence/behavior-models.json`。这些是固定小样本、单宿主机的正确性与恢复验收，不是分布式扩样、连续调度或实时新鲜度成绩。

## 血缘与治理验收（2026-09-11）

- `snow_models` 三个核心任务已自动记录 START / COMPLETE / FAIL；只在完成并校验后声明 Hive 表和文件输出。相同尝试身份稳定，重试产生独立 UUID；没有原始用户、请求、聊天或详细异常内容。
- 实际执行成功、失败、恢复及压缩资源配置四个 DAG。失败输入为不存在的固定清单，运营任务三次尝试均失败，行为与发布没有运行；旧模型发布保持有效。三次成功 DAG 的指标哈希均与此前基准相同。
- 共 24 条事件、12 次任务运行（9 完成、3 失败），全部通过官方 OpenLineage 2-0-2 JSON Schema 校验；本地开始/终态完整。普通异常和数据库不可写的隔离已测试，强制 kill 后遗留 START 的处理边界独立说明，未把超时推测当作失败事实。
- Marquez 0.50.0 / PostgreSQL 16.9 在分析 VM 2 GiB 实际启动，API 仅绑定 VM 回环。计算期间后端未启动，事件先进入本地 SQLite 日志。实际停机投递失败后 18 条待发记录保留；注入 HTTP 接收后、本地确认前故障，重试成功。
- 首批 18 条全部重放后，9 次运行状态及 16 节点/16 边的图相同。随后重建 PostgreSQL 与 Marquez 容器，持久卷中的状态与图保持一致。最后 6 条增量补发后，逐一读回全部 12 次运行的状态、输入和输出；控制端按序号与 SHA256 导入确认，最终 24 已确认、0 待发、0 未结束。
- 只覆盖固定输入清单、8 张验收 Hive 表、两个模型验收包及发布文件。Marquez 图是最近依赖，运行列表保留失败尝试；没有宣称 Kafka Topic 全链路、SQL 自动解析、父子运行或列级血缘。
- 治理容器上限为 Marquez 1 GiB、PostgreSQL 384 MiB；一次读回后观察分别约 258.7 / 37.66 MiB，分析 VM 可用约 1158 MiB，非负载峰值。新增镜像约占 0.83 GiB；`ods-compact` 3.5/3/1 GiB 已实际完成完整 DAG，运行项目文件约 58.79 GiB，仍保留 1 GiB 作业前门禁。3 GiB 计算 VM 一次运行观察可用 1555 MiB。
- Streamlit 新增 Marquez API 归档图与运行状态表，AppTest 和真实 Chromium SVG 渲染验证通过。该视图读取本机 JSON，不要求虚拟机在线。
- 收尾时 Airflow DAG 保持暂停，实验容器及三台 VM 已正常停止，未删除数据；项目文件约 51.30 GiB，宿主空闲约 58.25 GiB。三台 VM 已保存为 `ods-compact` 3.5/3/1 GiB 配置。

操作和可见缺口见[血缘手册](lineage.md)，实际收据见 `evidence/lineage.json`。生产产品和轻量接口没有新增依赖。

## 实时恢复与离线校正验收（2026-09-11）

- 新增独立合成 Kafka 与 Flink 实验部署，复用分析 VM 内的私有 Doris。控制/计算 VM 关闭，分析 VM 4.5 GiB；原 CDC Kafka 和业务服务未改动。
- 强制终止 TaskManager 后从 Checkpoint 恢复。17 条边界消息形成 9 有效、2 超迟到、3 重复、3 隔离，基础整数指标与独立基准一致；恰好 10 分钟迟到边界接收，早 1 毫秒隔离到迟到主题，跨日请求重复没有移动首次统计日期。
- 停止并启动 JobManager / TaskManager，重新上传相同 SHA256 的 JAR，明确从持久 Checkpoint 29 恢复；两级去重状态保留。追加两个重复和一个新事件后，累计 20 条输入、10 条实时事实。普通 session 无 HA，这次是人工恢复。
- 单独使用 Doris DUPLICATE KEY 探针验证 2PC：PRECOMMITTED 时不可见，ABORTED 后无数据，VISIBLE 后一行；重复 commit 返回 already visible，表中仍为一行，未依靠主键合并掩盖重复。
- 来源错误、契约错误、内容冲突共 3 条保留隔离选择清单。剩余 17 条送入实际 Spark 3.5.7 local[2]，得到 12 有效 + 5 重复；Parquet 四行日汇总逐项匹配独立基准。校正结果在独立 Doris 表中原子写入并重复发布，PV 从实时 7 补至 9，差额恰为两条超迟到事件。
- JM 512 MiB 配置、Java 11 时区解析、BE 低内存阈值及 session 重启后上传 JAR 丢失问题均在实测中发现并修复。最终 JM 进程 768 MiB，BE 容器 1792 MiB / 内部 1500M；这些是小样本条件，容器上限合计高于 VM 内存，未做峰值负载验收。
- 首次重建 Doris 时项目文件达 60.81 GiB，门禁拒绝发送验收事件；保留数据并回收空闲虚拟磁盘块后，实时验证约 58.17 GiB、校正发布约 58.35 GiB。压缩工具临时副本曾使宿主余量门禁失败，不能声明维护过程全程符合预算；新增默认只审阅、包含完整临时副本的压缩门禁。

- 最终配置启动、TaskManager 注册及既有 Doris 汇总读回通过，随后停止全部实验容器和三台 VM。数据与 Checkpoint 保留；关机后项目文件约 53.86 GiB，宿主空闲约 55.61 GiB。分析 VM 保存为 realtime 4.5 GiB，控制/计算为 3.5/3 GiB，但不能同时开启。

操作见[实时手册](realtime.md)，实际收据见 `evidence/realtime.json`。当前是合成小样本、单分区、单采集序列代际及分阶段校正；没有启用生产采集或把实时实验表接入公开页面。

## 本地端到端新鲜度与轻量独立验收（2026-09-11）

- 宿主真实 HTTP 合成采集经 SQLite FULL WAL、私有增量 HTTP、SHA256 归档、单分区 Kafka、Flink 和 Doris，300 条事件全部可见。发送至首次查询返回 P50 7.047 秒、P95 12.016 秒、P99/最大值 14.047 秒；该口径包含 HTTP、轮询和查询开销，是持久化接收至可见耗时的保守上界。202 返回至查询 P95 为 12.000 秒。
- 60 批各 5 条，实际发送阶段 84.218 秒，包含查询及资源检查开销，不是恒定 5 条/秒或持续生产负载。无启动积压，Flink 已就绪后计时；11 次 Checkpoint 完成、0 失败。使用同一宿主单调时钟，墙钟漂移约 0.0085 秒；VM 启动时偏差约 -0.295 秒。
- 接收、原始归档、Kafka、Doris 各 300 条，隔离/重复/超迟到均为 0。网站 PV 60 / UV 12；小吉终端 PV 60 / UV 10 / 完成请求 60 / 成功 48，与轻量库和独立 Python 基准逐项一致。
- 停止同步、取消合成作业并保存 Checkpoint 后，关闭所有数仓容器及三台 VM。宿主采集服务继续接收 5 条事件，原有 60 秒聚合线程推进至 305；公开合成行保持 0、快照时间更新。同步位点保持 300，新增事件等待未来同步，没有依赖数仓清理或聚合。
- 新增显式持续同步与目录目标绑定；复用 HTTP/Kafka 连接，错误退出并保留 pending。旧无绑定目录不被静默接管，更换 URL/目标/lane/来源须明确核对并迁移；同地址背后的数据源重建仍需人工识别。
- 单分析 VM 4.5 GiB，运行期间项目约 58.38 GiB；正常收尾后约 53.88 GiB，宿主空闲约 55.55 GiB。VM、原始数据、SQLite、归档和 Checkpoint 均保留；没有开启产品采集。

操作见[新鲜度手册](freshness.md)，实际证据见 `evidence/freshness.json`。本轮通过本地完整路径的小样本 P95 目标，生产公网、持续负载和故障期间 SLA 尚未验收。

## 尚未完成的验收

- 核心模型任务的显式血缘已通过；Kafka/CDC 落地、日指标/Doris 发布和 Flink 的血缘扩展、强制故障下成功终态核对仍待实施。入口采集、清单选择与资源阶段切换仍由操作者执行，尚未串成持续生产调度。
- 标准 6/6/10 GiB 三 VM 并发及负载峰值尚未验收；小配置 3.5/4/1 GiB 及 3.5/3/1 GiB 已通过固定样例。此前两台 6 GiB 节点启动后，追加 10 GiB 节点时可用 13819 MiB，低于 10240+4096 MiB 门禁要求，拒绝启动。没有绕过余量限制。
- Flink 恢复和本地 HTTP 接收至 Doris 小样本 P95≤60 秒已验证；生产公网部署及持续负载的 P95、Doris 扩样、多分区顺序、8 天 TTL 到期实测和连续运行仍待验收。离线/实时的分布式 10 万及 100 万规模测试尚未完成。
- Airflow 当前为单调度器/SQLite/SequentialExecutor 的实验部署；持续定时运行和多执行器不是本轮验收范围。Marquez 小样本读回/补发/容器重建已验证，治理扩样、Iceberg 集群并发与长期文件维护待验证；列级血缘未声明完成。
- 三 Kafka/ZooKeeper 专题配置已加入；HDFS 自动 HA 因 fencing 尚未配置而保持关闭。
- 真实日志跟随、线上 quota mount、代理/CSP 和业务生产发布；当前没有启用生产采集，也未修改生产服务或业务数据库。
- 7 天离线积压与 2 GiB 在线容量的真实容量测试、实时/离线/Doris 大规模一致性、物理资源测量和退出部署演练。新增镜像后，旧三 VM `ods-compact` 组合不能直接满足当前 60 GiB 预算；继续三节点并发前须先完成有明确范围的资源回收或重新验证配置。

首轮结束时项目文件长度约 31.19 GiB。离线闭环增加 Hadoop/Hive/Airflow 镜像和 HDFS/YARN 数据，运行期间已接近 60 GiB 门禁；文件长度是保守近似，不等于精确磁盘分配量。后续扩样须先处理受管理的临时缓存和磁盘回收，不能直接增加数据量或绕过门禁。

## 本轮修复的实际兼容问题

VMware e1000e 缺少 PCIe 插槽导致启动失败，生成配置改为有 PCI bridge 的 e1000；Windows 生成 env 文件强制 LF；MySQL 8.4 RSA 认证补齐 PyMySQL rsa 依赖；Kafka offset 元数据显式提供 leader_epoch；旧 Maven 3.6.1 改用校验下载的 3.9.9；Jackson 版本用 BOM 对齐；Spark 镜像内 Python 3.8 使用 timezone.utc。

虚拟机初始 UTC 快 8 小时，而 NTP 包计数为 0。新增只允许空闲实验机校时的工具和 2 秒偏差门禁；保留早期日志的原始时间，不将它们用于新鲜度测量。固定时钟的合成正确性样例不依赖这些日志墙钟。

Doris 官方入口需要可写运行配置，不能直接以只读配置覆盖；保留镜像默认配置并注入小内存参数，单独覆盖 JDK 17 堆大小。已有元数据启动时还需明确选择 NAT 地址，否则可能选到 Docker bridge 地址而无法恢复选主。上述修复已通过持久卷重建容器后的再次验收。Doris 镜像及工作数据促使分析节点系统盘由 18 增为 24 GiB 薄置备，项目物理预算不变。

MyWebsite 原有 npm 依赖审计报告 10 个问题（含 1 critical）；本次没有改动其依赖版本或将无关升级混入统计适配。生产候选应结合原项目维护处理。
