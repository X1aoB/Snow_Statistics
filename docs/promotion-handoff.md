# 小吉终端宣传录制移交包

状态：**2026-09-19 10:13 UTC 新版公网技术回归通过，录制固定为65fa0d0，可以开始录制。** 此包固定实际发布身份；运行观察和本地数仓验收可与制作并行。真实模型对白由用户录制，本轮技术验收没有调用付费模型，不能代替录制时对实际回复的检查。

## 版本与入口

| 项目 | 实际发布身份 | 已验证范围 |
|---|---|---|
| 小吉终端 1.0.0 | blue `65fa0d0cfb1e8ad8aafd090f88fab4364629757d` | 用户批准回执 `5d4038c6…` 后约10:11 UTC人工晋级；10:12:34 UTC新版公网Chrome与10:13 UTC纯GET回归通过 |
| 个人站 | `cf3d2fb0f7ddb3f5f897a4de5181083363d50c31`；Pages `765c7e23-8350-4f28-bc4b-aac52b0f7c05` | 正式 main、成功部署、两统计开关开启；保留新发布的文章和页面 |
| 小吉体验入口 | https://snow.xiaob.dev/ | 主界面、22 角色目录与选择、独立统计告知、允许/撤销 |
| 个人站 | https://xiaob.dev/ | 导航、入口跳转、统计/隐私设置、中英文及 393px 页面 |
| 公开统计页 | https://xiaob.dev/statistics/ | 延迟公开，今日各组等待公开；无数据与不可用不混淆 |
| 公开 JSON | https://stats.xiaob.dev/analytics/public/v2/summary.json | 独立 Tunnel、v2 分组及旧 v1 保护、精确 CORS、私有路径 404 |
| 小吉回滚锚点 | 停止保留的 green `0388b78b079011e28b772462b6c3634668856b59` | 此次仅晋级应用；共享embedding `465a1dbf…`未改变，不能用旧整份环境覆盖现状 |
| 个人站统计关闭锚点 | 同一 SHA、开关关闭的 Pages `4a332c46-0601-45a6-8713-fbbd31daa7d9` | 是配置关闭版本，不是旧源码版本；停用不删除历史库 |

公开技术回执只记录版本、状态和测试范围。私有令牌、Tunnel 凭据、原始事件、用户标识及生产备份不进入本移交包。

## 按原任务约定录制

形式为约 90 秒产品介绍：功能标题 → 实际操作 → 效果。用户录制真实操作和对白，原宣传任务负责剪辑、字幕、封面、配乐、三张贴吧图和文案。新增采购预算为零；测试不批量调用付费模型。

| 成片位置 | 核心画面 | 原片操作与建议时长 |
|---|---|---|
| 0–7 秒 | 小吉终端名称与主界面 | 主界面静止后自然操作，15–20 秒 |
| 7–19 秒 | 角色选择 | 浏览列表、搜索、选择；30–40 秒，角色总数以最终公开目录为准 |
| 19–35 秒 | 文字通讯 | 里芙的真实输入、发送及回复，30–60 秒 |
| 35–53 秒 | 面对面 | 芬妮和卜卜各录进入界面、实际互动、表情；各 30–60 秒 |
| 53–67 秒 | 上下文延续 | 胧嫣同话题两三轮真实问答，45–90 秒 |
| 67–79 秒 | 角色资料交流 | 辰星一段自己能够核对的设定话题，30–60 秒；不大段复述剧情 |
| 79–90 秒 | 体验入口和使用条件 | 主界面收尾，片尾由制作任务完成 |

优先 1920×1080、30fps，窗口尺寸/缩放保持一致。模型配置提前完成，密钥输入和账户配置不录入画面。每段前后留约三秒，保留实际等待与原始回复；后期缩短等待时标注“等待已缩短”。对白由用户构思，不强制按预设台词生成。

## 新增统计告知如何呈现

首次提示和正式 **设置 → 隐私 → 访问统计设置** 入口已经验收；右侧栏不再单独放统计按钮。两站分别选择，不以允许统计作为聊天的前置条件；测试浏览器最后已在两站撤销。可补录一段约 8–12 秒的设置演示，是否放进正片由节奏决定，使用说明和贴吧文案保留简短告知。

建议说明：

> 是否允许访问统计由你选择，可在“设置 → 隐私”中关闭；拒绝不影响聊天。不收集聊天内容或密钥。已记录生成请求的结果和耗时另行统计，不受这个开关影响。

公开页如无足够样本，应直接显示实际“等待公开”“样本不足”或无数据状态，不用假数字填画面。接口不可用应说明暂不可用，不能解释成零人使用。访问组按匿名标识判断，不等同自然人数；服务质量分母是**已记录的生成请求**，不等于所有 HTTP 请求或聊天尝试。样本门槛和延迟公开用于降低暴露风险，不保证完全匿名。

## 可以表达及需要避免的内容

- 角色目录、主界面、角色选择、统计选择与撤销已在正式页面验证。文字回复、面对面效果、上下文联系及角色资料准确程度，应由本次真实录制逐段核对；不能以技术测试或功能入口存在代替实际效果。字幕与对应真实画面一致。
- 片尾注明：非官方 AI 互动项目，需要连接自己的模型服务，费用或额度由对应账户承担。
- 不将有限上下文描述成永久记忆，不将角色资料辅助描述成回答绝对准确；不加入未开放的公网语音能力。
- 不把本地合成实验的百万条、恢复成绩或 P95 写成正式用户规模、生产 SLA 或物理机容灾能力。
- 统计页面不是核心聊天功能的依赖；电脑关闭或本地数仓停止不应被描述为产品停机。

## 本次验收与边界

新正式版本的67项纯GET检查全部通过，核对两站配置、公开目录、缓存/CORS、v1/v2保护与私有路径404。10:12:34 UTC的Chrome直接读取正式HTTPS和实际edge CSP，没有注入CSP；首次提醒、拒绝、允许、跨标签撤销及320/390/1440px界面通过。该轮4次统计请求在浏览器本地拦截为合成503，无付费模型调用，只完成一次无模型副作用的场景解析。不要把这轮技术回归说成真实模型效果评测或新增真实采集样本。

首次14:09–14:15香港上线窗口另有真实访问、角色选择与观察到的入口归因回执；分别撤销后继续导航/选择，没有因此新增采集。内部小计数、位点和数据hash不写入宣传材料。公开今日仍为`pending/null`，旧v1没有今日行；无需积累十个标识后才录制，不用假数据填充界面。早期73项激活前与67项激活后HTTP检查属于当时旧版本，不替代这次65fa0d0复验。

10:14:23 UTC切流后，质量reader/forwarder仍active、无重启，后台poll继续；8次socket探针最大0.094秒，历史失败计数37没有增加。质量ACK仍0，本轮没有真实付费生成完成样例，不能宣称质量统计覆盖全部生成请求。09:44:47 UTC的新备份完成且目录/仓库结构检查通过，但未做完整生产恢复演练。

本机正式私有同步、Flink/Doris与线上SQLite同截止整数对账，以及Checkpoint暂停、五引擎停止、analysis整台VM软关/开恢复已实测通过。它们是有限真实样本与固定版本的工程证据，不是用户增长指标、正式P95保证或物理机容灾。随后writer再次暂停，低内存离线、Hive和Iceberg继续独立验收，不影响已经可用的录制入口。

只把此包和可公开验收材料发送到已指定的“规划小吉终端宣传推广”任务，不转发私有运行目录。视频成品先审阅，具体发布账号、目标贴吧和最终发布操作继续由原任务落实。
