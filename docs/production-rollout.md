# 正式接入实施与验收记录

此文记录 2026-09-13 开始的正式接入工作。历史数仓报告继续表示合成实验；`source=real` 的代码分支测试仍明确使用隔离合成样例，不能写成真实用户规模。

## 2026-09-19 发布进展

**18:16 香港最新正式状态：** 小吉用户已批准新回执 `5d4038c6…`，按既有人工发布流程晋级 blue `65fa0d0cfb1e8ad8aafd090f88fab4364629757d`。持续统计入口移至“设置 → 隐私”，同意说明缩短并修复键盘焦点；原 green `0388b78` 停止保留回滚，embedding `465a1dbf…` 不变。PR/主线/签名发布证明均通过，实际候选验收及新鲜备份在晋级前完成。18:12–18:13 香港真正公网 Chrome、实际边缘 CSP、移动端与跨标签撤销、两站/collector 67 项只读 HTTP 检查通过；测试统计发送本地拦截，无付费模型调用。证据见[设置调整正式发布](evidence/production-settings-release-20260919.json)。下面05:42等记录保留首次接入过程，不是当前版本。

- 用户批准精确候选回执 `5a4305ae…` 后，小吉已在 05:42 UTC 从 blue `f3f7397` 晋级 green `0388b78b079011e28b772462b6c3634668856b59`。公开 build-info、live、角色目录和独立统计配置实际读回通过；原 blue 停止保留，共享 embedding 未再次变更，付费模型调用为零。
- 个人站保留独立主线新增内容后，PR #2 已合并。正式 SHA `cf3d2fb0f7ddb3f5f897a4de5181083363d50c31`，两个统计开关关闭的 Pages `4a332c46` 与开关启用后的 `765c7e23` 均构建成功。允许目录为 32 路径，collector 已精确对齐 22 角色及两 Origin。
- 新的独立统计 Tunnel 已接通 `stats.xiaob.dev`；TLS、两站 CORS、v1/v2 归档状态、no-store 及私有/诊断路径 404 实际通过。原业务 Tunnel 未修改。
- 正式个人站首次提示、拒绝后继续浏览、持续设置入口、393px 全部操作可见、Escape 恢复焦点、中英文统计标题/口径检查通过。初始 off 时显示归档；06:07:42–06:08:25 UTC 按冻结计划启用 lite 采集及两个日志服务，业务容器不变。
- 激活后实际浏览器分别同意，页面、角色选择及入口归因正常入库；两站跨标签撤销后继续导航/切换，06:15:38 UTC 位点和聚合位点仍相同。验收只产生少量真实操作事件，不向 real 库灌入合成样例。源站点击生成的一次性 URL 由受控浏览器另开标签验证，不能将此记录描述为 `target=_blank` 自动开窗已验证。
- 激活前 73 项、激活后 67 项 HTTP 检查通过。今日 v2 三组均 pending/null，v1 省略今日；历史 empty 日的 v1 零行是兼容投影，不是隐藏组旁路。当前页面显示等待公开或无数据，无原始标识。质量日志 ACK 仍为零；没有真实模型调用，不能宣称完成生成日志的端到端投递。
- 本轮完整本地检查为 Python 443 项、浏览器适配 23 项、Ruff 和 v1/v2 契约通过。`b977d91`、`86eb3bf`、`ea817d3` 的远端 CI 均成功，最后一个在 Java 11 实际验证外部偏移时间戳。

本节覆盖下方带日期的历史状态。[宣传录制移交](promotion-handoff.md)已经发送，真实数仓实测继续独立执行；首次启用时尚无真实生成样例，后续 10:56 UTC 已完成质量投递端到端核验，见下节。私有发布回执位于 `runtime/consent-candidate/` 与 `runtime/production-candidate/`；公开材料只引用哈希、版本和检查范围。

### 日志读取器运行观察与修复

08:27 UTC 的只读探针重现原读取器在 Docker 轮询期间不能响应 socket：一次请求在 3.002 秒超时，而 reader/forwarder 均无重启、spool 无事件、没有 reader 完整性错误。这是轮询与服务串行造成的通信问题，不是已确认的事件丢失或业务生成故障。

修复提交 `34d7b989a5b94fab2d31ff9b6104072ead0040f7` 使用一个后台任务执行有界 Docker 轮询，主线程继续独占 SQLite 和 socket；没有并发轮询队列，没有扩大权限或读取范围，没有延长转发器超时。合成实际 socket 回归验证慢轮询仍能读取已提交批次、失败保留覆盖状态和既有计数。完整远端 CI 通过。

按冻结计划 `f77136aca5f9ed1d4e7103137e0abd712b2fd777b495d66333efdcd877780367` 于 08:36:12 UTC 完成一文件升级。reader SHA256 为 `3a50ad75936e17bcfea458789a5fa29d3953592610defdf613db645c9d6ac32a`；只停启两个统计日志单元，原权限、限额、配置与全部业务容器保持。45 秒内 113 个实际 Unix socket 请求最大 0.045 秒，轮询和转发进度更新，无新增失败；历史 37 次失败保留。原文件及独立 `recover` 入口保留，恢复不依赖业务容器连续性，停止/启动均核验进程身份。

私有回执 `reader-upgrade-applied-20260919.json` 不含事件或标识。此时真实生成 ACK 仍为 0；只有实际生成完成后才能补记质量链路的端到端成绩。

10:14 UTC 新应用蓝绿切换后再次只读检查，两统计日志服务正常、无重启，后台轮询继续，历史37次失败没有增加；8次 socket 探针最大0.094秒。没有新的真实生成ACK，不把跨版本正常轮询写成已记录全部请求。

10:40 UTC 后续只读观察两服务仍 active，失败累计保持 37，8 次 socket 探针最大 0.001 秒；仍无真实生成完成 ACK。未重启服务、调用模型或导出事件正文，私有回执为 `quality-observation-20260919T104034.json`。

用户随后在正式站完成正常对话并确认收到回复。10:54 UTC 观察到新的真实完成日志 ACK，10:56 UTC 在服务器只读事务中进一步验证：已确认的 reader 投影与 collector 持久化事件字段严格一致，只有完成事件白名单字段；请求去重状态存在、聚合位点已越过该事件，受影响日期的请求量和成功数与独立 SQL 读回一致。只返回验证布尔结果，不导出事件、用户标识、请求编号或真实小计数，也不读取聊天正文。验证器没有调用模型或修改生产。见[真实完成日志证据](evidence/production-quality-completion-20260919.json)。该成功样例不证明全部请求覆盖、付费模型成本或生产 P95，公开小样本规则保持。

### 本地正式实时恢复与离线准备

签名源码 `63a0d415` 的实际正式 epoch 完成原初始化中断恢复、私有同步、Kafka/Flink/Doris读回和同截止SQLite整数对账。随后Checkpoint24暂停、全部五个实时引擎停止、整台VM软关/开机，并从同一Checkpoint恢复，原存储身份及到期时间不变，指标没有重复累加；后续再次暂停保留状态。

离线小资源候选在 compute 最小锁定Python环境准备后，使用2048/1920/768MiB运行56条固定**合成**样例，YARN `application_1789812889866_0001` 得到28有效＋28重复，与独立Parquet/整数基准一致；项目峰值63.62GiB，未触发资源停止或OOM。此成绩只证明小样例离线配置，不能替代真实ODS、Hive窗口、闭日报表或生产新鲜度验收。正式首个香港日期9月19日尚未闭合，首个真实闭日报表须在9月20日00:00后计算。

随后独立执行真实 ODS：已捕获批次实际落入 HDFS，非空文件与快照哈希校验、compute/analysis 两副本读回通过。10:29 UTC 切回原 Kafka 存储完成 ACK，并独立核对真实提交位点与 HDFS 已验证末端，pending 清除。终态所有 VM 软关闭、原数据保留；该闭环有自己的[实际证据](evidence/real-production-loop-20260919.md)，与上段合成 golden 分开记录。

## 2026-09-13 安装边界（历史）

统计服务已在现有服务器独立安装，保持 `SNOW_MODE=off`。独立 512 MiB ext4 状态挂载已启用；collector 与网关运行，网关只监听宿主 `127.0.0.1:8110`。日志读取、转发、正式采集和独立 Tunnel 尚未启用，业务 Tunnel 未改变。

已验证安装镜像：`sha256:cd5e3c2f3aa6ebe35c7e765de0339e1581d25d7f857cad288c56d0aa8f91dd50`。冻结 collector 源包：`8089cf1b59f22fbdd44b15ceb543b0b8994661f44c4134a55eae8cfe3173d7f8`。这是实际安装身份，不代表此后所有本地实验代码已经发布。

实际检查：off 时公开 v1/v2 为归档，采集返回 503；网关拒绝私有事件、健康、文档及额外路径；精确 CORS 预检通过、不允许的来源被拒绝；在线备份回读完整；缺失持久挂载拒绝启动。独立 synthetic HTTP collector 验证入库去重、Authorization 剥离、私有路由封闭与故障，不向正式库灌入测试数据。正式库验收时事件数为 0。

私有技术回执保存在忽略目录 `runtime/production-candidate/`，包括 `off-acceptance.json`、`synthetic-acceptance.json`、`final-runtime.json`、`installed-source-manifest.json`。这些文件不进入公开仓库。部署步骤与具体资源见 [production README](../deploy/production/README.md)。

## 用户可见功能

- 两站访问统计需主动同意；独立命名空间保存选择，支持到期、GPC/DNT、存储不可用、跨标签页撤销、清空队列及取消在途请求。关闭不修改业务存储。
- 非阻断提示在 320px 和 1440px 实际 Chromium 中检查；个人站配合现有中英文。公开路径/角色列表与实际候选目录一致。
- 小吉完成日志使用专用白名单 INFO logger，不启用全局详细日志。质量统计分母为“已记录的生成请求”，不表示所有 HTTP 或所有聊天尝试。
- summary v2 按访问、质量、热度表达 `published/suppressed/pending/empty`，隐藏值为 null；旧 v1 整行保护，私有精确汇总与位点状态需要 reader token。
- 香港日期 D 在 D+3 00:15 起按固定截止时间冻结公开。访问/热度按有效匿名标识门槛 10；质量按请求量及成功、失败小计数门槛整组判断。公开状态 26 小时过期，长时间开页重新判断；失败不伪造刷新或零值。

产品接入来源：[MyWebsite #2](https://github.com/X1aoB/MyWebsite/pull/2)、[Project_Snow #59](https://github.com/X1aoB/Project_Snow/pull/59)。两产品本次正式发布已完成，实际版本和时间见[9月19日发布进展](#2026-09-19-发布进展)。后续发布仍遵循各自流程：个人站 main 合并触发生产构建；小吉需要精确候选回执和人工晋级，候选 CI 不能代签。

### 2026-09-13 候选预览与共享维护（历史）

当时个人站候选为 `6b17f426222f295850d4b608295f214d85689be5`，Pages deployment `03b67947-8e13-4b6d-a408-7b8bbab4f298` 实际构建成功。在用户完成现有 Access 登录后，实际预览通过中文/英文标题、指标分母说明、接口不可用且无零值行、393px 页面不横向溢出、表格独立横向滚动以及 Escape 关闭设置并恢复焦点检查。该次预览没有启用采集，也没有把 Tunnel 不可用状态视为公网接入验收。私有回执：`runtime/production-candidate/website-preview-6b17f42.json`。它保留预览证据，不替代 9 月 19 日正式 Pages `765c7e23` 的验收。

小吉 main `0388b78b079011e28b772462b6c3634668856b59` 的完整 CI 和 release-proof 当时已通过。既有共享 embedding 的系统包修复用独立容器实测：九个模型文件 hash 一致，四条合成输入各 512 维，最大绝对误差 0；实际停止、重启和再次比对通过。新鲜 pinned 备份、旧镜像保留和原 API 健康已核验。用户明确批准维护窗口后，9 月 13 日 13:55:35.880–13:55:50.262 UTC 实际替换/核验通过，新镜像 `465a1dbfa32e9c28f1e12cac9b96318ac804923428ff3d25a23eb463c0546471` 生效，原 API 容器和其他容器未变；该区间不是精确业务中断观测。当日应用候选 stage 仍在进行、人工晋级尚未完成；9 月 19 日已按新的精确候选批准完成晋级。私有回执 `runtime/consent-candidate/embedding-0388b78-applied.json`；SHA256 `ba8e788d60ed5ac55fdc4b26b636ecbb8c83f892a4b54d32a33ed4b1a789b759`。这个小样本不代表任意输入的数学等价或性能保障。

## 实際引擎验收：隔离合成输入、真实代码分支

VM 使用 scale 2048/2048/1024 MiB，control / compute / analysis；Kafka 捕获阶段与 Spark 阶段切换运行。保留 64 GiB 项目、35 GiB 宿主空闲、4 GiB 可用 RAM 门禁。

| 项目 | 实际结果 |
|---|---|
| 隔离收集与同步 | 140 条合成样例，经 loopback collector → 校验归档 → `snow.real.fixture_prod01.events.v1`；正式请求 0 |
| Kafka / HDFS | 一个输入分区，offset 140；HDFS 校验后才 ACK。v2 清单将位点头与有效批次窗口分开 |
| Spark 日指标 | YARN `application_1789302517038_0001`；140 有效、0 重复/隔离，6 行日指标与独立基准一致 |
| Spark 行为 | YARN `application_1789302517038_0002`；44 会话、14 转化，5 组结果逐行与基准一致 |
| 留存 | 2 个分群的 D1 因覆盖不足为 null；D7 尚未成熟。没有倒填覆盖时间或称为自然新增用户 |
| Iceberg | YARN `application_1789302517038_0003`；daily/session_daily/retention/funnel 共 6/6/2/3 行，当前和历史快照全量读回一致；新增字段验证通过 |
| ODS 到期 | 仅该 fixture 模拟 7 日到期，实际删除 HDFS 旧批次与旧快照，读回旧快照不可达；offset 140 及 head 保留，新窗口 0 批次 |
| 私有看板 | AppTest 用同组 fixture 检查真实分支 4 个表，覆盖不足显示原因；读取前清理受管聚合副本 |

这轮 Spark 没有开启 Hive 注册；Iceberg 没有声明 Hive 或列级血缘。新真实 Kafka/Doris/Flink epoch 的独立配置与生命周期需单独实测，不能用上述离线成绩替代实时恢复和性能验收。真实生产 P95 尚无证据。

本轮私有回执：`runtime/production-acceptance/{daily,behavior,lake-receipt,ack,lake-cleanup,ods-expiry-acceptance}.json`。用于测试的合成标识留在忽略目录，不进入学习手册或公开 API。

2026-09-13 后续补齐采集源 instance/generation 的全程绑定后，使用新 `fixture_prod02` 重新运行，不改写上述旧证据：YARN `application_1789302517038_0004` / `0005` / `0006` 分别完成日指标、行为、Iceberg，140 条输入和全部黄金结果相同。新清单、覆盖声明、输出包都绑定同一 collector 身份；新私有看板按当前严格契约再次通过四表及“历史覆盖不足”提示。旧版缺少代际的包现在被明确拒绝，不能沿用旧读取成绩。

新 fixture 同样完成实际 HDFS 到期删除与 offset 140 保留；其回执位于 `runtime/production-acceptance/fixture02/`。随后只启动 2 GiB 分析 VM 的治理服务，将两次实际 Iceberg 执行自动产生的 4 条 OpenLineage 元数据投递到 Marquez，逐个读回 2 次运行的状态与输入输出，最新图为 6 节点/5 边。投递副本已确认 4 条；源日志确认导入待下一次控制机启动，不把副本确认冒充原日志已更新。没有新增 Kafka、Hive 或列级血缘声明。

独立 epoch 的物理清理还在 1 GiB 分析 VM 用合成标记完成了实际删除回读：模拟到期和约 45 秒真实截止各移除 1 容器、5 卷，真实截止 PID1 退出 78，原有其他容器与卷清单不变。它证明精确回收边界，不证明 Kafka/Flink/Doris 运行性能，也不是 VMDK/SSD 取证级擦除。汇总证据见 [production-integration.json](evidence/production-integration.json)。

## 生命周期与运行方式

见 [真实生命周期操作](real-lifecycle.md)。原始 7 天、辅助 30 天、聚合 90 天均依据原始时间。私有同步先绑定实例代际和位点；HTTP 410、源重建、未聚合输入丢失或保留期缺口会停止并生成元数据缺口回执。

显式新起点恢复使用 `snow-stats rebase-sync --help`，必须填写新目录、新 lane、当前合法位点与简短原因。旧日志保留，新通道不能伪装成连续历史；不会自动从 earliest 跳过缺失。

私有发布保存受 `RealLifecycle` 管理的不可变汇总文件，`real-latest.json` 仅保存位置、校验和、到期时间；看板每次读取先清理再校验。停止并不全删历史；关机期间不能即时物理删除，开机先清理再开放读取。

## 已完成的接入门禁与仍待验证的范围

截至[9月19日发布进展](#2026-09-19-发布进展)，独立 Tunnel/DNS、两产品精确候选测试与发布、小吉人工晋级、采集/质量转发启用、实际公网版本、同意/撤销及私有隔离回归已经完成。出现新故障时按实际变更关闭相应统计开关或回滚独立发布，不重复执行旧候选计划。

真实私有同步、Kafka/Flink/Doris、同截止 SQLite 整数对账、整机暂停/恢复，以及 HDFS 两副本落地至原 Kafka 确认闭环已实际通过，详见[9月19日真实闭环记录](evidence/real-production-loop-20260919.md)。用户正常对话的真实完成日志已单独通过转发、去重、聚合验证；本地原前缀对账不包含这次较晚的事件，后续同步需明确新的截止。自然闭日模型、Hive/Iceberg、生产 P95 和长期样本不足项目仍须各自补充执行证据。存储容器健康、配置存在或合成 CI 通过均不能替代这些结果。

公网回归后已移交“规划小吉终端宣传推广”，见[移交记录](promotion-handoff.md)。移交不代表视频已制作或发布；素材只能使用已验证版本及真实操作，公开样本不足就录制真实等待状态，不伪造统计。

## 学习手册

独立目录：`C:/Users/25685/Desktop/简历及相关材料/Snow_Statistics数据开发学习手册/`。38 个正文专题、总目录与校验记录；SQL 练习和链接已检查。目录、部署、真实同步、发布章节最终仍须按正式回执校准。其他简历材料未覆盖。
