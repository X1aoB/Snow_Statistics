# 2026-09-13 安全停点

用户要求“再次提出签名请求，然后在安全的地方停止任务”。本记录是暂停交接，不是正式接入计划已经完成的声明。

## 已保存的版本

- Snow Statistics 实现：`be591517e77068c29c3556eb3e5f5770c9d2fd12`，GPG 签名验证 G，已推送独立分支 `codex/production-integration`，没有合并 main。
- MyWebsite 候选：`6b17f426222f295850d4b608295f214d85689be5`；Pages preview `03b67947-8e13-4b6d-a408-7b8bbab4f298`。用户完成现有 Access 登录后，中英文、393px 移动端、统计不可用且无伪造零值、键盘设置操作通过。正式 main 没有发布。
- 小吉候选：`0388b78b079011e28b772462b6c3634668856b59`，已 stage 为 green；正式流量仍是 blue `f3f739773aacd79cb850bf134abee43adf2c0fd0`。内部健康、目录和配置检查通过，首次体验/公告之后的完整浏览器同意流程尚未形成通过回执。没有人工晋级。
- 新共享 embedding：`sha256:465a1dbfa32e9c28f1e12cac9b96318ac804923428ff3d25a23eb463c0546471` 已按本次明确维护授权生效；新服务和原 API 健康通过，其他容器未变。应用升级和共享维护是不同步骤。

## 停机事实与保留范围

14:05:24 UTC，实际 VMware 运行列表为零；三台项目 VM 全部关闭，analysis 使用 soft stop。关机前 Docker 运行容器列表为空。本次没有删除数据：06 失败实验的三个停止容器和三个同代卷、历史数据、源码包、固定 JAR、配置及日志都保留。没有创建 07，也没有把未到期标记改成到期。

宿主观察：项目文件 58.85 GiB，磁盘空闲 55.67 GiB，可用内存 16037 MiB。虚拟磁盘容量 29 GiB 与项目实占是不同口径；下次仍须通过 64 GiB/35 GiB/4 GiB 三项硬门禁。

私有回执：`runtime/epoch-acceptance/safe-stop-06-{host,guest}.json`；现场元数据归档 SHA256 `387a7aef5b0241067f3d33d50e8a5ccc4ce92c682a62eda1931f46394785a039`。`fixture-engine-06.failed-retirement-scope.json` 只生成审阅范围，SHA256 `fa0b1f89747a359c12a5ea2c9fa06d97864af1ba620ca97c334ee7e2a9506a0d`，未执行删除。

线上 collector/统计网关保留独立安装，采集 off。独立 stats Tunnel 尚未创建，日志转发尚未启用；产品原有访问保护保持。重新创建 Tunnel 需恢复时办理尚未完成的具体操作确认，不把先前预览登录或 embedding 维护批准混用。

14:05:17 UTC 的服务器实际读回确认 blue/green 完整健康均正常、embedding healthy、collector off 且正式事件为 0、两个日志 unit 均 inactive、公开快照 archived；本次测试 SSH 隧道已关闭、浏览器测试进程为零。回执位于 `runtime/consent-candidate/staged-0388b78-{safe-stop,local-stop}.json`。green 保留为候选，不伪造“全部通过”的人工验收回执。

## 测试状态

本地 Python 336 项通过、1 条依赖弃用提示；浏览器适配 23 项通过，Ruff 与 v1/v2 契约通过。实际 Java JAR 仍为 `ef25755bf473610b47e62c4392b720c00d84fef23bb3ee5c567d7d21dbba0913`。BE512 修改后针对测试 24 项通过。

新提交远端 [CI 34761600822](https://github.com/X1aoB/Snow_Statistics/actions/runs/34761600822) 和 [CI 34761598677](https://github.com/X1aoB/Snow_Statistics/actions/runs/34761598677) 已结束失败：Flink 构建成功，python-browser 在 `uv run pytest -q` 失败。当前只核验到失败步骤，未定位具体 Linux 测试；不要把 Node action 弃用提示当成 pytest 的已确认原因。按用户停止要求，没有在停点继续启动修复。

Doris 06 实测越过 scanner 和 AgentServer，但后续 `PInternalService → WorkThreadPool / BRpcService` 仍出现线程创建 error 11，BE pids512 不足以覆盖未显式限制的后续默认池。没有进入 SQL/topic/事件 bootstrap，不存在本轮完整实时/session恢复通过成绩。下一步应先核查剩余按 CPU 自动扩大的池，已知 heavy/light BRPC 池的有限配置尚未实施，不能直接重跑同一配置或继续扩大内存/磁盘。

## 恢复时的顺序

1. 先读本记录、Git 当前状态和远端失败日志；修复实际 CI 差异。不要覆盖保留证据或将未接线草稿放回正式路径。
2. 核对服务器真实 active/green、共享镜像和统计 off 状态。所有准备材料有时间边界，过期备份或漂移 plan 需刷新；不能沿用旧“已准备”回执直接切流。
3. 恢复本地前先核对磁盘/RAM及原到期时间。到期资源先按登记清理再开放读取；06 保留状态若要提前收尾，需按精确失败合成范围处理，不进行全局 prune。
4. 完成受限配置的独立合成引擎验收及同 epoch 恢复。再冻结源码/JAR，创建真实生产 writer，不能将 fixture 登记冒充正式 writer。
5. 完成独立 Tunnel、两产品候选完整回归与明确人工晋级；随后启用采集、公开保护与公网回归。公网通过后才将 [宣传移交包](promotion-handoff.md) 发给既定宣传任务。
6. 真实 Hive 尚无可运行统一入口；未完成源码已放在 `runtime/hive-draft/20260913-220517/`，具体缺口见 [Hive 交接](hive-next-steps.md)。手册最终版本仍需与真实发布校准。

学习手册位于 `C:/Users/25685/Desktop/简历及相关材料/Snow_Statistics数据开发学习手册/`，40 个 Markdown 文件。它明确区分候选、合成实测和未验证能力，暂停不覆盖其他简历资料。
